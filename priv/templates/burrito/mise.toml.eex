# Toolchain pin for <%= @app %> burrito builds.
#
# Burrito 1.6 requires zig exactly 0.16.0 — newer or older versions are
# rejected at `mix release` time. (0.15.2 cannot link native binaries on
# macOS 26 "Tahoe": the SDK 26 change breaks its linker — ziglang/zig#31658.)

[tools]
zig = "0.16.0"
